/** @jsx React.DOM */

var React = require("react");

var AboutView = React.createClass({

    render: function() {
        return (
            <h2>About</h2>
        );
    }
});

module.exports = AboutView;